# Git Flow Básico
Comando para el flujo de git basico
```sh
git status
git add . o git add -a
git status
git commt -m "Descripcion del cambio en el proyecto"
git push
```
## Nota:
Si no me dice quien soy, entonces necesito restaurar la PC.
